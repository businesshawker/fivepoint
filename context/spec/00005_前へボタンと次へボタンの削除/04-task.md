# タスクリスト: 前へボタンと次へボタンの削除（/task - 00005）

1. 実装: src/fivepoint.html:194〜197 の div class="nav" ブロック（prevBtn/nextBtn含む）を削除する
1. 実装: src/fivepoint.html:112〜117 の .nav および .nav .arrow のCSS定義を削除する
1. 実装: src/fivepoint.html:736〜737 の prevBtn/nextBtn クリック用イベントリスナを削除する
1. 実装: src/fivepoint.html:739〜745 の keydown ハンドラから ArrowLeft/ArrowRight の分岐を削除し、0〜5 のみ処理する
1. 実装: 左右キー移動に関するコメント/説明文を更新し、数字キー(0〜5)のみ記載にする
1. 実装: prevBtn/nextBtn への参照が残っていないことをリポジトリ全体で検索し除去する
1. テスト: 画面DOMに #prevBtn と #nextBtn が存在しない（document.getElementByIdがnullを返す）
1. テスト: 画面DOMに .nav コンテナが存在しない
1. テスト: CSSに .nav / .nav .arrow の定義が残っていない（テキスト検索ベース可）
1. テスト: 左右キー(←/→)を押しても画像の表示インデックスが変化しない
1. テスト: 数字キー(0〜5)入力で選択中画像の評価が対応値に変更される
1. テスト: サムネイルクリックで該当画像に正しく切り替わる
1. 回帰テスト: フォルダ選択→読み込み→サムネイル表示→画像表示の一連の流れが問題なく実行できる
1. 回帰テスト: 実サイズ表示のクリック切替とドラッグ移動(パン)が従来通り動作する
1. 回帰テスト: 画像のドラッグ&ドロップ追加で通番が末尾+1となり、必要に応じてJPEG変換される
1. 回帰テスト: 一連の操作後もコンソールエラーが出力されない
1. 境界テスト: 最初の画像表示中に ← を押しても何も起きずエラー/警告が出ない
1. 境界テスト: 最後の画像表示中に → を押しても何も起きずエラー/警告が出ない
