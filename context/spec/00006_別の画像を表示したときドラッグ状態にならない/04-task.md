# タスクリスト: 00006 別の画像を表示したときドラッグ状態にならない

1. `ImageZoomController` に `resetDragState()` を追加（座標とフラグ初期化、`viewer--dragging`除去）
2. `selectIndex(i)` 冒頭で `zoom_controller.resetDragState()` を呼び出す
3. `selectIndex(i)` の表示切替で直前状態を保持し分岐（実サイズなら `applyActualSizeMode()`、それ以外は `applyFitMode()`）
4. 実サイズ表示の初期オフセットとクランプ処理が既存通り動作することを確認（目視・ログ）
5. 受け入れテスト: 実サイズで画像Aドラッグ中に画像Bへ切替後、追加クリックなしでBがドラッグ移動すること
6. 退行テスト: フィット表示での切替後もフィット維持、クリックでの実⇄フィットトグルが従来通り
7. 退行テスト: ドラッグ開始しきい値4px・移動クランプが従来通り（小移動で未開始、大移動で開始）
8. 退行テスト: ズーム、評価変更、フォルダ切替、D&D追加にエラーが出ない（コンソールエラーなし）
